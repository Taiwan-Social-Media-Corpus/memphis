import UserRepository from './user.repository';
import AdminRepository from './admin.repository';

const repositories = [UserRepository, AdminRepository];

export { UserRepository, AdminRepository, repositories };
